<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.earthquake" >
    <uses-sdk android:targetSdkVersion="19"
            android:minSdkVersion="13"/>
    
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo">
        <!--android:theme="@style/AppTheme" >-->
        <activity
            android:name=".main"
            android:label="@string/app_name"
            android:uiOptions="splitActionBarWhenNarrow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".preferences_Activity" android:label="Настройки"></activity>
        <activity android:name=".FragmentPreferences"/>

        <provider
            android:authorities="com.paad.earthquakeprovider"
            android:name=".EarthQuakeProvider"/>

        <receiver android:name=".EarthQuakeAlarmReciever">
            <intent-filter>
                <action android:name="com.paad.earthquake.ACTION_REFRESH_EARTHQUAKE_ALARM"/>
            </intent-filter>
        </receiver>

    
        <activity android:name=".EarthQuakeSearchResults"
            android:label="Earthquake Search"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"
                />
        </activity>
        
        <service android:name=".EarthQuakeService" android:enabled="true"/>

    </application>

</manifest>
